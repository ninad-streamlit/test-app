# Streamlit Secrets Configuration
# This file is used for local development
# For Streamlit Cloud, use the Secrets section in the app settings

# OpenAI API Key (optional - can also use .env file)
# OPENAI_API_KEY = "your-api-key-here"

# Google OAuth (optional - can also use .env file)
# GOOGLE_CLIENT_ID = "your-client-id"
# GOOGLE_CLIENT_SECRET = "your-client-secret"
# GOOGLE_REDIRECT_URI = "http://localhost:8501"

# Note: For local development, you can use a .env file instead
# The app will automatically fall back to environment variables if secrets are not found here
